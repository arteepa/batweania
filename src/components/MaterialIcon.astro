---
interface Props {
  name: string;
  class?: string;
  fill?: boolean;
}

const { name, class: className = "", fill = false } = Astro.props;
const style = fill ? "font-variation-settings: 'FILL' 1;" : undefined;
---

<span class:list={["material-symbols-outlined", className]} style={style}>{name}</span>
