SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
# From first `sanity deploy` output (avoids prompting on later deploys).
SANITY_STUDIO_APP_ID=

# Same as root .env: SANITY_PROJECT_ID and SANITY_DATASET are accepted as fallbacks.
